<footer>
  <div id="footer">
    <p class="text-muted credit">

        <div class="btn-group" id='float-left'>
            <button id='button-left' type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
              <img id='gear' src='/icons/setting.png'> <span id="span-this"> Norway, Borealis </span>
            </button>
            <ul class="dropdown-menu drop-up" role="menu">
                <% if current_user.nil? == false %>
                <li><a href="#"><%= current_user.email%></a></li>
                <% end %>
                <li class="divider"></li>
                <li><% if current_user.nil?%>
                      <%= link_to "Sign In", login_path %>
                    <% else %>
                      <%= link_to "Sign Out", logout_path %>
                    <% end %>
                </li>
            </ul>
        </div>

        <div class="btn-group" id='float-right'>
            <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                Create Task <span class="caret caret-up"></span>
            </button>
            <% if current_user.nil? %>
              <ul class="dropdown-menu drop-up" role="menu">
                  <li><p style='color: black;'>Please sign in before you create a list</p></li>
              </ul>
            <% else %>
              <ul class="dropdown-menu drop-up" role="menu">
                  <li><%= link_to "Create List", new_user_list_path(current_user.id)%></li>
                  <li><%= link_to "Create List", new_user_list_path(current_user.id)%></li>
                  <li><%= link_to "Create List", new_user_list_path(current_user.id)%></li>
                  <li class="divider"></li>
                  <li><%= link_to "Create List", new_user_list_path(current_user.id)%></li>
              </ul>
            <% end %>
        </div>
    </p>
  </div>
</footer>

<script> 
$( "#button-left" ).click(function() {
  $('#gear').animate({  borderSpacing: -90 }, {
    step: function(now,fx) {
      $(this).css('transform','rotate('+now+'deg)');  
    },
    duration:'slow'
},'linear');
});

</script>
